# Outillage local Claude Code (ne pas publier sur GitHub)
.claude/

# Secrets : NE JAMAIS COMMITTER
# (notamment la cle du Firebase Admin SDK prevue pour le script local)
serviceAccountKey.json
*serviceAccount*.json
.env
.env.*
jeton*
*token*.txt
*.pem
*.key

# Script objectifs/ : sauvegardes = donnees perso reelles, cles = secrets (repo PUBLIC)
objectifs/sauvegardes/
objectifs/config.local.json

# Dependances / OS / editeurs
node_modules/
.DS_Store
Thumbs.db
.vscode/
.idea/
